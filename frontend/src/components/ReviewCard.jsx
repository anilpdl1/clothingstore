import {useState} from 'react';import RatingStars from './RatingStars';import api from '../services/api';import {useAuth} from '../context/AuthContext';export default function ReviewCard({review,onChanged}){const {user}=useAuth(),[error,setError]=useState('');const vote=async value=>{try{if(review.my_vote!==null&&review.my_vote!==undefined)await api.delete(`/reviews/${review.id}/helpful`);else await api.post(`/reviews/${review.id}/helpful`,{vote:value});onChanged()}catch(e){setError(e.response?.data?.detail||'Sign in to vote')}};const report=async()=>{const reason=window.prompt('Reason: Spam, Offensive content, Fake review, Irrelevant, Harassment, or Other');if(!reason)return;try{await api.post(`/reviews/${review.id}/report`,{reason});setError('Report received.')}catch(e){setError(e.response?.data?.detail||'Could not report')}};return <article className="border-b py-6"><div className="flex items-center justify-between"><RatingStars value={review.rating} small/><span className="text-xs text-stone-500">{new Date(review.created_at).toLocaleDateString()}</span></div><h3 className="mt-2 font-bold">{review.title}</h3><p className="mt-1 text-stone-700">{review.comment}</p><div className="mt-3 flex items-center gap-3 text-sm"><span className="font-medium">— {review.user_name}</span>{review.verified_purchase&&<span className="rounded-full bg-[#d9dfd0] px-2 py-1 text-xs">✓ Verified purchase</span>}</div><div className="mt-4 flex gap-3 text-sm"><span>Helpful?</span><button onClick={()=>vote(true)}>👍 {review.helpful_count}</button><button onClick={()=>vote(false)}>👎 {review.unhelpful_count}</button>{user&&<button onClick={report} className="ml-auto text-stone-500 underline">Report</button>}</div>{error&&<p className="mt-2 text-xs text-stone-500">{error}</p>}</article>}

import {useEffect,useState} from 'react';import RatingStars from './RatingStars';import api from '../services/api';import {useAuth} from '../context/AuthContext';export default function ReviewForm({productId,onCreated}){const {user}=useAuth(),[orders,setOrders]=useState([]),[form,setForm]=useState({order_id:'',rating:5,title:'',comment:''}),[message,setMessage]=useState('');useEffect(()=>{if(user)api.get('/orders').then(r=>setOrders(r.data.filter(o=>o.payment_status==='PAID')))},[user]);if(!user)return <p className="mt-6 rounded-xl bg-stone-100 p-4 text-sm">Sign in after purchasing this product to leave a verified review.</p>;const submit=async e=>{e.preventDefault();try{await api.post(`/products/${productId}/reviews`,{...form,order_id:+form.order_id});setMessage('Thank you — your verified review is live.');onCreated()}catch(err){setMessage(err.response?.data?.detail||'Could not submit review')}};return <form className="mt-7 rounded-2xl bg-stone-100 p-5" onSubmit={submit}><h3 className="text-lg font-bold">Write a review</h3><p className="mt-1 text-sm text-stone-600">Only paid purchases are eligible; we verify this on the server.</p><select required className="mt-4" value={form.order_id} onChange={e=>setForm({...form,order_id:e.target.value})}><option value="">Select your order</option>{orders.map(o=><option value={o.id} key={o.id}>{o.order_number}</option>)}</select><div className="mt-4"><RatingStars value={form.rating} onChange={rating=>setForm({...form,rating})}/></div><input required className="mt-3" placeholder="Review title" value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/><textarea required className="mt-3 min-h-28 w-full rounded-xl border p-3" placeholder="How did it fit and feel?" value={form.comment} onChange={e=>setForm({...form,comment:e.target.value})}/><button className="mt-3 rounded-full bg-ink px-5 py-2 text-white">Submit review</button>{message&&<p className="mt-3 text-sm">{message}</p>}</form>}

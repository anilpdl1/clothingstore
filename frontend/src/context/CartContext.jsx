import {createContext,useContext,useEffect,useState} from 'react';import api from '../services/api';import {useAuth} from './AuthContext';
const Cart=createContext();export const useCart=()=>useContext(Cart);export function CartProvider({children}){const {user}=useAuth(),[cart,setCart]=useState({items:[],total:0});const refresh=()=>user?api.get('/cart').then(r=>setCart(r.data)):setCart({items:[],total:0});useEffect(()=>{refresh()},[user]);const add=async(v,q)=>{setCart((await api.post('/cart/items',{product_variant_id:v,quantity:q})).data)};const update=async(id,quantity)=>setCart((await api.put(`/cart/items/${id}`,{quantity})).data);const remove=async id=>setCart((await api.delete(`/cart/items/${id}`)).data);return <Cart.Provider value={{cart,refresh,add,update,remove}}>{children}</Cart.Provider>}
